# deps / build
node_modules
build
.react-router
.wrangler
worker-configuration.d.ts

# env — never commit credentials. .env is NOT ignored by default in most
# scaffolds; that gap is how keys end up in git history.
.env
.env.*
!.env.example
*.local

# logs / os / editor
logs
*.log
npm-debug.log*
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.suo
*.sw?

# legacy Bolt app keeps its own ignores
legacy/node_modules
legacy/dist
