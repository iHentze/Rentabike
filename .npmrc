; npm's strict peer resolution rejects a couple of ranges that resolve fine
; at runtime (react-router 8 / vite 8 / vitest 4 pool). Install the way the
; repo was bootstrapped so `npm ci` behaves the same everywhere, CI included.
legacy-peer-deps=true
